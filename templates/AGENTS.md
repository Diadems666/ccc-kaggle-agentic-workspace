# Project Context for AI Agents

<!-- Copy this file to your project root as CLAUDE.md (for Claude Code) or AGENTS.md -->
<!-- Fill in the sections below. The agent reads this at session start. -->

## Project Overview

**Name**: [project name]  
**Purpose**: [1-2 sentence description of what this project does]  
**Status**: [active development / maintenance / prototype]

## Tech Stack

- **Language**: [Python 3.12 / TypeScript 5 / etc.]
- **Framework**: [FastAPI / Next.js / Django / etc.]
- **Database**: [PostgreSQL / SQLite / etc.]
- **Tests**: [pytest / jest / etc.]
- **Package manager**: [pip + requirements.txt / npm / poetry / etc.]

## Repository Structure

```
src/
├── [key directory]    — [what's in it]
├── [key directory]    — [what's in it]
tests/
└── [what tests exist]
```

## Code Conventions

- [e.g., "Use async/await throughout — no synchronous DB calls"]
- [e.g., "Type hint every function parameter and return value"]
- [e.g., "PEP 8, enforced by ruff"]
- [e.g., "Tests must pass before committing (pytest -x)"]

## Key Files

- `[path/to/file]` — [what it does and why it's important]
- `[path/to/file]` — [what it does and why it's important]

## What NOT to Do

- [e.g., "Never run `DROP TABLE` or destructive migrations without explicit confirmation"]
- [e.g., "Don't add dependencies without discussing first"]
- [e.g., "Don't push directly to main — use PRs"]

## Running the Project

```bash
# Install dependencies
[install command]

# Run tests
[test command]

# Start dev server
[start command]
```

## Current Focus

[What is currently being worked on? What problem are we solving right now?]
